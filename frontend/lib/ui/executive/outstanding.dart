import 'package:flutter/material.dart';

class ExecutiveOutStanding extends StatelessWidget {
  const ExecutiveOutStanding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Outstanding"));
  }
}
